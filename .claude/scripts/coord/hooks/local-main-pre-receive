#!/usr/bin/env bash
# The local bare mirror is a fetch source, never a delivery destination.
echo "local main mirror: push rejected — enqueue the committed head and deliver it through mc-train." >&2
exit 1
