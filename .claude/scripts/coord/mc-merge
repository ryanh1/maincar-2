#!/usr/bin/env bash
# Compatibility shim with no delivery behavior. The historical implementation
# lives under tests/fixtures so no environment variable can revive it here.
set -euo pipefail
echo "mc-merge has been replaced by mc-deliver." >&2
echo "  Use mc-deliver enqueue, then mc-deliver run." >&2
exit 2
